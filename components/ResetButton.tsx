import React from "react";

const ResetButton = () => {
  return (
    <button
      type="submit"
      className="bg-white border border-secondary text-primary w-fit p-2 rounded-lg hover:bg-secondary hover:text-white"
    >
      reset
    </button>
  );
};

export default ResetButton;
